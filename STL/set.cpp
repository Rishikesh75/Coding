/*
    ---Set STL Meathods---
    s.size();
    s.insert(val);
    s.count(val);
    s.find(val);
    s.size();
    s.empty()
    s.LowerBound()
    s.UpperBound()
    s.begin(),s.end()
    s.clear()
*/

#include<iostream>
#include<set>
using namespace std;
int main()
{
    set<int>s;
    s.insert(1);
    s.insert(1);
    return 0;
}